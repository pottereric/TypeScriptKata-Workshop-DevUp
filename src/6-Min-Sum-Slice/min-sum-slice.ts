/*
Given an array of numbers and a length (where length is guaranteed to be 
    less than the array's length), return a slice of the specificed length with 
    the minimum possible sum. Maintain the order of the original array! 
*/

export const READY_TO_TEST = false; // SET ME TO TRUE IF YOU DARE

export function minSumSlice(input: number[], length : number): number[] {
  return [0];
}